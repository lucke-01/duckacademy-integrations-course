 # get posts and authors
 query allPosts{
     posts {
         id,
         title,
         author {
             id,
             name
         }
     }
 }
 # author by id
 query authorById {
     authorById(authorId: "Author0") {
         id,
         name,
         posts {
             id
         }
     }
 }