Created basic communication client -server OK

TODO:

    - Make a cron to send to the client more notifications.

    - Make another use case where the server will notify the client given one minute for example.